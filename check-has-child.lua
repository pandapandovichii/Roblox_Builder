-- check for child in a location
hasChild = part:FindFirstChild("otherPart") -- put the name of the child here

--if the child is found, hasChild will == True
--if the child is not found, hasChild will == False
